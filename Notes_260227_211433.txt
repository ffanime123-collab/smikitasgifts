<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smikita's Gift</title>
    <style>
        :root {
            /* Keep her accents pink/gold, but set everything else dark */
            --pink: #ff4d6d;
            --soft-pink: #ffb3c1;
            --gold: #ffb703;
            --text: #ffffff; /* White text for readability */
            --overlay: rgba(0, 0, 0, 0.6); /* Semi-transparent dark overlay */
            --glass: rgba(0, 0, 0, 0.75); /* Darker glass effect for message boxes */
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: var(--text);
            text-align: center;
            overflow-x: hidden;
            
            /* ---- THIS IS THE KEY MUSTANG GT BACKGROUND SECTION ---- */
            background-color: black; /* Fallback if image fails */
            background-image: linear-gradient(var(--overlay), var(--overlay)), url('mustang.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed; /* Keeps the car still when she scrolls */
            /* --------------------------------------------------------- */
        }

        .section {
            display: none;
            min-height: 100vh;
            padding: 20px;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            animation: fadeIn 1.5s ease-in;
        }

        .active { display: flex; }

        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        h1, h2 { text-shadow: 2px 2px 10px rgba(0,0,0,0.8); }

        /* Welcome Page Styles */
        .btn-enter {
            padding: 15px 40px;
            font-size: 1.2rem;
            background: var(--pink);
            color: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(255, 77, 109, 0.5);
            transition: all 0.3s ease;
            margin-top: 20px;
            text-transform: uppercase;
            font-weight: bold;
            letter-spacing: 1px;
        }
        .btn-enter:hover { transform: scale(1.1); box-shadow: 0 6px 20px rgba(255, 77, 109, 0.8); }

        /* Cake Styles (No major changes) */
        .cake { position: relative; width: 150px; height: 100px; background: #6d4c41; border-radius: 10px 10px 0 0; margin-top: 50px; border: 2px solid rgba(255,255,255,0.2); }
        .candle { position: absolute; width: 10px; height: 40px; background: #fff; top: -40px; left: 70px; }
        .flame { 
            position: absolute; width: 15px; height: 25px; background: var(--gold); 
            border-radius: 50% 50% 20% 20%; top: -25px; left: -2px;
            animation: flicker 0.1s infinite alternate;
            box-shadow: 0 0 15px var(--gold);
        }
        @keyframes flicker { from { transform: scale(1); opacity: 0.9; } to { transform: scale(1.2) rotate(3deg); opacity: 1; } }
        
        /* Message Pages - Glassmorphism style to contrast against the Mustang */
        .message-box {
            max-width: 650px;
            background: var(--glass);
            padding: 40px;
            border-radius: 20px;
            border: 1px solid rgba(255,255,255,0.1); /* Subtle edge */
            box-shadow: 0 15px 35px rgba(0,0,0,0.5);
            line-height: 1.8;
            margin-bottom: 30px;
            backdrop-filter: blur(5px); /* Frosty effect on the background image */
        }
        
        .message-box h2 { color: var(--soft-pink); margin-top: 0; }
        .romantic-text { font-style: italic; color: #ffccd5; }

        /* Gallery */
        .gallery-title { margin-top: 40px; }
        .gallery { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; width: 90%; max-width: 1200px; padding-bottom: 50px;}
        .gallery img { 
            width: 100%; height: 250px; object-fit: cover; border-radius: 15px; 
            border: 4px solid rgba(255,255,255,0.2); box-shadow: 0 8px 25px rgba(0,0,0,0.5); 
            transition: all 0.3s ease;
        }
        .gallery img:hover { transform: scale(1.05) translateY(-5px); border-color: var(--pink); }

    </style>
</head>
<body>

    <div id="page1" class="section active">
        <h1>Hello 👋🏻, Welcome Smikita</h1>
        <button class="btn-enter" onclick="nextPage(2)">Enter</button>
    </div>

    <div id="page2" class="section">
        <h2 id="blow-text" style="color: var(--soft-pink);">Blow the Candle in 3...2...1</h2>
        <div class="cake" onclick="blowCandle()">
            <div class="candle">
                <div class="flame" id="flame"></div>
            </div>
        </div>
        <p style="margin-top: 20px; color: #aaa;">(Click the flame to blow it out!)</p>
    </div>

    <div id="page3" class="section">
        <div class="message-box">
            <h2>Happy Birthday, Smikita! 🎂</h2>
            <p>Today is all about you! I want to wish you the happiest of birthdays. May this year be filled with success, laughter, adventures, and all the happiness in the world. You deserve the best, and I hope this small website brings a smile to your face today...</p>
            <button class="btn-enter" onclick="nextPage(4)">My Message</button>
        </div>
    </div>

    <div id="page4" class="section">
        <div class="message-box romantic-text">
            <h2>To My One & Only ❤️</h2>
            <p>Smikita, I wanted to tell you that you are my everything. Finding you has been the greatest adventure of my life. I promise to support your dreams (even the wild ones!), stand by your side, and love you a little more every single day. I love you more than words can express...</p>
        </div>
        
        <h3 class="gallery-title">Memories With You</h3>
        <div class="gallery">
            <img src="https://ibb.co/TqB4kPr3.jpg" alt="Smikita 1">
            <img src="https://ibb.co/DHNV475s.jpg" alt="Smikita 2">
            <img src="https://ibb.co/5wvgB3K.jpg" alt="Smikita 3">
        </div>
    </div>

    <script>
        function nextPage(pageNumber) {
            document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
            document.getElementById('page' + pageNumber).classList.add('active');
            window.scrollTo(0,0); // Scroll to top when page changes
        }

        function blowCandle() {
            document.getElementById('flame').style.display = 'none';
            document.getElementById('blow-text').innerText = "Make a wish, My Love! ✨";
            
            // Wait 2 seconds, then auto-advance
            setTimeout(() => {
                nextPage(3);
            }, 2500);
        }
    </script>
</body>
</html>